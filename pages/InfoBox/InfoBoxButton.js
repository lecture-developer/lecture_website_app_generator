import { buttonTypes } from '../../resources/constants';

class InfoBoxButton {
  constructor(text, type, link) {
    this.text = text;
    this.type = type;
    this.link = link;
  }
}

export default InfoBoxButton;